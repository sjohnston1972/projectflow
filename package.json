{
  "name": "projectflow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ProjectFlow task management target application for Darwin.",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev -w @darwin/projectflow-app",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build -w @darwin/shared && npm run build -w @darwin/telemetry-client && npm run build -w @darwin/projectflow-app",
    "verify": "npm run typecheck && npm run test && npm run build"
  },
  "engines": { "node": ">=20.0.0" }
}
